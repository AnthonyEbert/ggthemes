% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/matlab.R
\name{post_matlab}
\alias{post_matlab}
\title{Make plot look like a Matlab plot}
\usage{
post_matlab(obj, xlim = c(NA, NA), ylim = c(NA, NA), base_size = 12,
  base_family = "")
}
\arguments{
\item{obj}{ggplot object}

\item{xlim}{NULL or numeric vector of length 2 provides c(from, to) for x-axis}

\item{ylim}{NULL or numeric vector of length 2 provides c(from, to) for y-axis}

\item{base_size}{base font size}

\item{base_family}{base font family}
}
\description{
Make plot look like a Matlab plot
}
\examples{
x <- rnorm(100)
y <- rnorm(100)

obj <- data.frame(x = x, y = y)

post_matlab(
  ggplot(obj) +
    aes(x = x, y = y) +
    geom_point(),
  c(-3, 3), c(-3, 3), 12
) + stat_smooth(se = FALSE)

}
